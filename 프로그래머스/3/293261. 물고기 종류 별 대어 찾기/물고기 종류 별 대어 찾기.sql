SELECT
    ID,
    FISH_NAME,
    LENGTH
FROM
    (SELECT
        ID,
        FISH_TYPE,
        LENGTH,
        RANK() OVER(PARTITION BY FISH_TYPE ORDER BY LENGTH DESC) AS NO
     FROM FISH_INFO
    ) FI
    JOIN 
    FISH_NAME_INFO
    USING(FISH_TYPE)
WHERE
    NO=1
ORDER BY
    ID ASC


